package A2Z;

import javax.swing.*;

/**
 * Created by Nardeen on 15-Dec-17.
 */
public class Vproduct {
    JLabel label1=new JLabel("Enter product Name :");
    JLabel label2=new JLabel("Enter product ID :");
    JLabel label3=new JLabel("Enter product brand name :");
    JLabel label4=new JLabel("Enter product brand id :");
    JLabel label5=new JLabel("Enter product model name :");
    JLabel label6=new JLabel("Enter product model id :");
    JLabel label7=new JLabel("Enter product price :");
    JLabel label8=new JLabel("Click On Any Button !!");
    JTextField StoreName=new JTextField(50);
    JTextField StoreID=new JTextField(50);
    JTextField newStoreName=new JTextField(50);
    JButton Add = new JButton("Add Store");
    JButton Remove = new JButton("Remove Store");
    JButton Update = new JButton("Update");
    JButton Back = new JButton("Back");
    StoreOwner myStoreOwner;//=new StoreOwner();
}
